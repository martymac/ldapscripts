TODO (ideas) :
**************

- Add an option to modify the home dirs (LDAP + directory + rights) of a user when renaming it ?
- Add an option to modify related groups (memberUid's) when renaming/deleting a user/machine ?
- Add an option to block deleting a group if it is a user's primary group
